#include<stdio.h>
#include<string.h>

void segment(const char* str,int len){

}

int main(void){
	char str[100];
	int segmentLength;
	printf("Enter the string of 0s and 1s: ");
	scanf("%s",str);
	printf("Enter the Segment length: ");
	scanf("%d",segmentLength);
	

	segment(str,segmentLength);
	return 0;
}
	
